spring:
  application:
    name: leo-laptop
  profiles:
    active: dev
  security:
    user:
      name: root
      password: root
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB